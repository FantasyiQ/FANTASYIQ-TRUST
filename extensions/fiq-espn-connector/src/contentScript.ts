// FiQ ESPN Connector — content script (passive mode)
// Runs on ESPN pages. No active behaviour needed — the background service
// worker handles all cookie reads. This file exists as an extension point
// for future active-mode features (e.g. an inline "Connect to FiQ" banner).

export {};
